<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.UserMapper">
    <select id="findByUsername" resultType="com.example.demo.User">
        SELECT username, password, name,id_card as idCard,phone,email FROM user WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.example.demo.User">
        INSERT INTO user (username, password,name,id_card,phone,email) VALUES (#{username}, #{password},#{name}, #{idCard},#{phone}, #{email})
    </insert>

    <update id="changeInfo" parameterType="com.example.demo.User">
        UPDATE user SET password=#{password},name=#{name},id_Card=#{idCard},phone=#{phone},email= #{email} WHERE username=#{username}
    </update>


</mapper>